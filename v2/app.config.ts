export default defineAppConfig({
  ui: {
    // 'cepreuna' es la paleta de marca definida en app/assets/css/main.css (@theme).
    colors: {
      primary: 'cepreuna',
      neutral: 'slate'
    },
    button: {
      defaultVariants: {
        // botones algo más compactos, acorde al panel
      }
    }
  }
})
